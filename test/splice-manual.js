//Generar manualmente al el metodo de SPLICE

'use strict'
const months = ['Jan', 'March', 'April', 'June'];

const newSplice= (array, start, num, add) => {
    let arr = [];
    for (let x = 0; x < array.length; x++) {
        if (start > array.length) start = array.length;
        if (num === 0) arr


    }
}




months.newSplice(months, 1, 0, 'Feb');

months.newSplice(months, 4, 1, 'May');
